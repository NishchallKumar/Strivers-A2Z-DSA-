class Solution {
    public static int largest(int[] arr) {
        // code here
        int idx = arr[0];
        for(int i = 0; i < arr.length; i++){
            if(arr[i]>idx){
                idx=arr[i];
            }
            
            
        }
        return idx;
    }
}
